
import { PermissionType, Reason, Compensation, CompensationMethod } from './types';

export const PERMISSION_TYPE_OPTIONS = Object.values(PermissionType);
export const REASON_OPTIONS = Object.values(Reason);
export const COMPENSATION_OPTIONS = Object.values(Compensation);
export const COMPENSATION_METHOD_OPTIONS = Object.values(CompensationMethod);
